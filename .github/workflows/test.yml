name:Test

on:
  push:
    branches:
      - 'main'
  pull_request:
    branches:
      - 'main'

jobs:
  test:
    runs-on: ubtuntu-latest
    steps:
      - name: Test steps
        run: echo "Test Phone Book"
